# Writing the provided content to a text file
content = """
Hướng Dẫn Tải Dữ Liệu Từ Cuộc Thi Kaggle "NYC Taxi Trip Duration"

1. Đăng nhập và Tham gia Cuộc Thi
- Bước 1: Truy cập vào trang cuộc thi: https://www.kaggle.com/competitions/nyc-taxi-trip-duration/data.
- Bước 2: Nhấn vào nút "Join Competition" (tham gia cuộc thi). Bạn sẽ cần đồng ý với các điều khoản và chính sách của Kaggle để có quyền tải dữ liệu.

2. Tạo API Token (Kaggle.json)
Sau khi tham gia cuộc thi, bạn cần tạo một token API để có thể tải dữ liệu qua API.

- Bước 1: Truy cập vào trang cấu hình tài khoản của bạn https://www.kaggle.com/account.
- Bước 2: Cuộn xuống phần API và nhấn vào "Create New API Token". Điều này sẽ tải về một tệp có tên "kaggle.json", chứa các thông tin đăng nhập của bạn.

3. Cài đặt Kaggle API và Cấu Hình
- Bước 1: Cài đặt Kaggle API nếu bạn chưa cài:
    pip install kaggle
- Bước 2: Đặt tệp "kaggle.json" vào thư mục "~/.kaggle/" (trên hệ điều hành Unix) hoặc thư mục "C:\\Users\\<Your-Username>\\.kaggle\\" (trên Windows).
    - Để dễ dàng hơn, bạn có thể di chuyển tệp "kaggle.json" vào thư mục "~/.kaggle/" (trên Linux/Mac) hoặc "C:\\Users\\<Your-Username>\\.kaggle\\" (trên Windows). Nếu thư mục này chưa tồn tại, bạn có thể tạo nó.

4. Tải Dữ Liệu Bằng Kaggle CLI
- Bước 1: Mở terminal (hoặc command prompt) và nhập lệnh sau để tải dữ liệu:
    kaggle competitions download -c nyc-taxi-trip-duration
    Lệnh trên sẽ tải về tất cả các tệp dữ liệu từ cuộc thi về máy tính của bạn.
- Bước 2: Sau khi tải xuống, bạn sẽ có các tệp .zip. Giải nén các tệp này và bạn sẽ thấy các tệp dữ liệu dưới định dạng .csv, chẳng hạn như train.csv, test.csv.

5. (Tuỳ Chọn) Sử Dụng Python và Kaggle API
Nếu bạn muốn tải dữ liệu trực tiếp từ Python thay vì qua terminal, bạn có thể sử dụng Python cùng với Kaggle API:
    import kaggle

    # Tải dữ liệu cuộc thi NYC Taxi Trip Duration
    kaggle.api.competition_download_files('nyc-taxi-trip-duration')
    Lệnh trên sẽ tải về tất cả các tệp dữ liệu từ cuộc thi.

Lưu Ý:
- API Quota: Kaggle API có giới hạn tải xuống (quota). Nếu bạn gặp lỗi liên quan đến quota, bạn sẽ phải đợi hoặc tải xuống từng tệp riêng lẻ.
- Giải Nén: Sau khi tải xong, bạn có thể giải nén các tệp .zip bằng các công cụ giải nén như 7-Zip hoặc qua Python:

    import zipfile

    with zipfile.ZipFile('nyc-taxi-trip-duration.zip', 'r') as zip_ref:
        zip_ref.extractall('path_to_extract')

Nếu bạn cần thêm hỗ trợ về việc xử lý dữ liệu hoặc xây dựng mô hình, đừng ngần ngại yêu cầu thêm thông tin!
"""

# Save to a .txt file
file_path = "/mnt/data/kaggle_nyc_taxi_trip_duration_instructions.txt"
with open(file_path, "w", encoding="utf-8") as f:
    f.write(content)

file_path
